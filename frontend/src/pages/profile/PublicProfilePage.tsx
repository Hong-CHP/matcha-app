import type { PublicProfile } from "@/types/user"
import { useState } from "react"

function PublicProfilePage() {
    const [targetProfile, setTargetProfile] = useState<PublicProfile>(null)
    return (
        <>
        
        </>
    )
}

export default PublicProfilePage